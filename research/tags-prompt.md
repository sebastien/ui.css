Can you organise the of all HTML tags in groups of categories, for instance:

- `controls` (UI-related input)
- `structure` (footer, navigation, etc)
- `text` (paragraphs, lists, tables, emphasis, etc)

If an HTML tag does not fit in a group, create a new group. Please format the
output like so:

```
group: tag, tag
```

for instance

```
text: p, h1, h2, ul, li
```

Can you categorise all HTML tags as explained above?

